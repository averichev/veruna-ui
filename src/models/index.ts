export interface IUserListItem {
  username: string;
  id: string;
}

export interface ISiteListItem {
  id: string;
  name: string;
  description: string;
  domain: string;
}

export interface IPageListItem {
  id: string;
  name: string;
  code: string;
}
